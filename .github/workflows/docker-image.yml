name: Docker Image Build and Push

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

env:
  IMAGE_NAME_DOCKERHUB: jianjungki/funasr
  IMAGE_NAME_GHCR: ghcr.io/jianjungki/funasr
  BUILD_CONTEXT: examples/openai_api
  DOCKERFILE: examples/openai_api/Dockerfile

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - device: cpu
            image_suffix: cpu
            base_image: python:3.10-slim
          - device: cuda
            image_suffix: gpu
            base_image: nvidia/cuda:12.9.0-runtime-ubuntu22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver: docker-container

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ${{ env.BUILD_CONTEXT }}
          file: ${{ env.DOCKERFILE }}
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            FUNASR_DEVICE=${{ matrix.device }}
            BASE_IMAGE=${{ matrix.base_image }}
          tags: |
            ${{ env.IMAGE_NAME_DOCKERHUB }}-openai-${{ matrix.image_suffix }}:${{ github.sha }}
            ${{ env.IMAGE_NAME_DOCKERHUB }}-openai-${{ matrix.image_suffix }}:latest
            ${{ env.IMAGE_NAME_GHCR }}-openai-${{ matrix.image_suffix }}:${{ github.sha }}
            ${{ env.IMAGE_NAME_GHCR }}-openai-${{ matrix.image_suffix }}:latest
